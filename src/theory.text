npm install -D tailwindcss
npx tailwindcss init

#useRef hook
 const email = useRef(null);
 <input ref={email} type="email" placeholder="enter your email">
 email.current => give the refrence to the input box
 email.current.value => give the value written inside the box

 useRef provide refrence to the input box
 we are using firebase for authentication

#use formik for form validation


#authentication - we used firebase for authentication as backend




steps for Deployment
0.install firebase cli - 'npm install -g firebase-tools'
1.firebase login - 'firebase login'
2.initialize firebase - 'firebase init'
3. Deploy command - 'firebase deploy'

Redux
npm install @reduxjs/toolkit
npm install react-redux

